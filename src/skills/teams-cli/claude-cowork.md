# Claude Cowork

Read and follow the [complete teams-cli instructions](instructions.md).

In Claude Cowork, the normal shell may not be the user's computer. If `teams-cli` is not available there and computer use is enabled, open PowerShell on the user's computer and run the documented commands there. Never ask the user to attach or expose `.teams-cli`, tokens, cookies, or browser state. If neither a local command environment nor computer use is available, explain that teams-cli requires Claude Code running locally or direct console use.
